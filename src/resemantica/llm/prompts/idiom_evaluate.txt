# version: 1.3

## TASK
IDIOM_EVALUATE

## ANALYST INSTRUCTION
Reason through the task once. Do not restart your reasoning, loop over the same uncertainty, or narrate self-corrections such as "but wait", "let me rethink", or "I should check again". After one reasoning pass, return only the required output.
If uncertain after one pass, choose the safest schema-valid value and return the JSON array. Do not explain uncertainty.

## INSTRUCTIONS
Evaluate each Chinese candidate phrase from a web novel.
Decide whether it is an idiom, chengyu, proverb, or fixed expression.

Return compact JSON only. Do not include markdown, prose, explanations, analysis, chain-of-thought, or <think> text.

For each candidate, return:
- candidate_id
- is_idiom: true or false
- usage_type: literal, idiomatic, mixed, or unknown
- translation_strategy: literal, idiomatic, or preserve
- reason_code: lexicon_match, chengyu_pattern, fixed_expression, repeated_non_compositional, ordinary_noun_phrase, proper_name, compositional, insufficient_evidence, or eval_error
- confidence: number from 0.0 to 1.0
- meaning_zh: short Chinese meaning if known, otherwise empty string

## OUTPUT FORMAT
Return a JSON array only. No markdown fences. No explanation text.

## CANDIDATES
{CANDIDATES_JSON}
